rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // This portal is app-gated because it uses Discord ID + PIN instead of email/password.
    // For production-level security, move privileged writes behind Cloud Functions or Firebase Auth custom claims.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
